# NUT Docker Config

Dockerized NUT server for APC Smart-UPS and AP7920 PDU with SNMP support. LLM Generated.
## Setup

1. Clone repo:
   ```bash
   git clone https://github.com/<username>/nut-config.git
   cd nut-config